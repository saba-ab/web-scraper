# web-scraper for ambebi.ge

# first clone this repository with command

git clone https://github.com/saba-ab/web-scraper.git
cd web-scraper

# install dependencies to run this code

npm install

# then you can start the scraper by running:

node scraper.js
